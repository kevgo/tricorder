name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: make setup
      - run: make test
      - run: make fix
      - run: git diff HEAD --exit-code --color
