use camino::Utf8PathBuf;

/// provides all files in the current working directory
pub(crate) fn all_files() -> Vec<Utf8PathBuf> {
    // recursively walk the current working directory and collect all files
}
